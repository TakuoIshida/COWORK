<%= form_for(work_place) do |f| %>
  <% if work_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_place.errors.count, "error") %> prohibited this work_place from being saved:</h2>

      <ul>
      <% work_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :region %>
    <%= f.text_area :region %>
  </div>

  <div class="field">
    <%= f.label :tel %>
    <%= f.number_field :tel %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_area :url %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
